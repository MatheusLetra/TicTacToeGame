import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFF',
    alignItems: 'center',
  },
  logo: {
    width: 600,
    height: 600,
    resizeMode: 'center'
  },
  playbutton: {
    width: 100,
    height: 100,
    resizeMode: 'center'
  }
})

export { styles }