import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 90,
    height: 140,
    backgroundColor: '#FFF',
    alignItems: 'center',
    justifyContent: 'center'
  },
  value: {
    color: '#CB6CE6',
    fontWeight: 'bold',	
    fontSize: 40,
    textAlign: 'center'
  }
})

export { styles }